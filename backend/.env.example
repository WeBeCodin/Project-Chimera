# Copy this file to backend/.env and fill in your values

# Database (Supabase PostgreSQL - free tier)
DATABASE_URL="postgresql://postgres:[password]@[host]:5432/[database]"
# Alternative: Use Vercel Postgres if preferred
# POSTGRES_PRISMA_URL="postgresql://username:password@hostname:port/database?pgbouncer=true&connect_timeout=15"
# POSTGRES_URL_NON_POOLING="postgresql://username:password@hostname:port/database?connect_timeout=15"

# NextAuth.js Configuration  
NEXTAUTH_SECRET="your-secret-key-here-generate-a-secure-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Vercel Blob Storage (replaces S3)
BLOB_READ_WRITE_TOKEN="your-vercel-blob-token"

# AWS Configuration (Optional - for advanced processing workflows)
# Only needed if you want to use AWS Step Functions for video processing
# AWS_REGION="us-east-1"
# AWS_ACCESS_KEY_ID="your_access_key_id"
# AWS_SECRET_ACCESS_KEY="your_secret_access_key"
# AWS_ACCOUNT_ID="your_aws_account_id"
# STEP_FUNCTIONS_ARN="arn:aws:states:region:account:stateMachine:name"

# Legacy S3 Configuration (Optional - can be removed)
# S3_BUCKET_NAME="chimera-videos-${AWS_ACCOUNT_ID}"

# For production deployment
# NEXTAUTH_URL="https://your-production-domain.vercel.app"